# Staging

This folder is the **inbox**, not the catalog clients download.

Community PRs add or change addon entries in `staging/<flavor>/<category>.json`. After a maintainer or agent reviews the linked source for safety, approved entries are copied into `<flavor>/<category>.json` at the repository root.

Do not point addon clients at these files.
